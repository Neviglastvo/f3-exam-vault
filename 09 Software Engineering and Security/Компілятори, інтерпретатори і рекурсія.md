---
topic: "Компілятори, інтерпретатори і рекурсія"
section: "Програмування і інформаційна безпека"
status: unseen
priority: P0
source_page: "Білети 9, 13"
source_document: "F3 ІПММС.docx"
tags:
  - exam/f3
  - section/software-engineering
---

# Компілятори, інтерпретатори і рекурсія

## Конспект розгорнутої відповіді

- Компілятор перекладає програму з вихідної мови в іншу форму до виконання: проміжне подання, машинний код або байткод. Типовий конвеєр: лексичний аналіз → синтаксичний → семантичний → побудова проміжного подання → оптимізація → генерація коду.
- Інтерпретатор виконує програму або її проміжне подання під час роботи. Межа не абсолютна: віртуальні машини можуть комбінувати байткод, інтерпретацію та JIT-компіляцію.
- Лексичний аналіз перетворює символи на токени, синтаксичний перевіряє структуру за граматикою, семантичний — типи, області видимості та інші правила мови. Усі три етапи мають різні типи помилок.
- Рекурсія — виклик функцією самої себе через базовий випадок і рекурсивний крок. Базовий випадок зупиняє обчислення, а кожен крок має наближати до нього; інакше виникає нескінченна рекурсія або переповнення стека.
- Рекурсивний розв'язок доречний, коли структура задачі природно розкладається на менші підзадачі. Його порівнюють з ітерацією за читабельністю, пам'яттю стека й часовою складністю, а не за самим фактом рекурсії.
- **Перевірка:** [[Перевірені джерела для ІПММС#Програмування, web і безпека]].

> [!summary] Відповідь за 20–30 секунд
> Компілятор аналізує вихідний код і перекладає його в іншу форму до виконання або для подальшого виконання; інтерпретатор виконує програму через проміжне подання чи безпосередньо. Рекурсивна функція викликає себе для меншої підзадачі й мусить мати базовий випадок.

## Суть
- Етапи компіляції: лексичний аналіз → синтаксичний аналіз → семантичний аналіз і перевірка типів → проміжне подання → оптимізація → генерація коду/зв'язування. Реальні системи можуть поєднувати ці етапи.
- Інтерпретатор зручний для інтерактивного запуску й переносимості, але конкретна продуктивність залежить від реалізації: байткод, [[Глосарій#JIT|JIT]]-компіляція й кешування стирають просте протиставлення.
- Рекурсія коректна, коли задача розкладається на менші екземпляри й є базовий випадок. Глибока рекурсія витрачає стек; іноді її замінюють явним стеком або циклом.
- Рекурсивні алгоритми природні для дерев, графів, divide-and-conquer і рекурентних визначень, але треба аналізувати складність і дублювання підзадач.

## Іспитовий фокус
- **Що запам'ятати:** компілятор та інтерпретатор — способи реалізації мови, а не взаємовиключні властивості самої мови.
- **Типова пастка:** рекурсивний виклик без базового випадку або без зменшення задачі веде до нескінченної рекурсії/переповнення стека.
- **Розгорнута відповідь:** конспект вище порівнює способи виконання програми, етапи трансляції та умови коректної рекурсії.

## Пов’язані знання
- [[Мови програмування і структура програм]]
- [[Рекурентні співвідношення]]
- [[Формальні мови й автомати]]

## Самоперевірка
- **Поясни без нотатки:** шлях коду від тексту програми до машинного виконання.
- **Назви 2–3 ключові поняття або кроки:** лексема, синтаксис, тип, байткод, базовий випадок.
- **Порівняй із найближчою пов’язаною темою:** рекурсія і ітерація.

## Джерело програми
- [[F3 ІПММС.docx]]: білети **9, 13**.
- Назва/склад теми взяті з переліку білетів; пояснення — стислий навчальний виклад.
